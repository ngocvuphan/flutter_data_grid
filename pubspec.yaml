name: vph_data_grid
description: Flutter DataGrid is used to display and manipulate data in table view with advanced features.
version: 0.0.2
homepage: https://github.com/ngocvuphan/flutter_data_grid

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  linked_scroll_controller: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
